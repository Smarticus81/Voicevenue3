import type { Viewport } from 'next';

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  themeColor: '#10b981',
  viewportFit: 'cover',
};

export default function NotFound() {
  return null;
}


