{
  "name": "orders.add_item",
  "description": "Add item(s) to a tab; creates an order record and decrements inventory by recipe.",
  "parameters": {
    "type": "object",
    "properties": {
      "tab_id": { "type": "string" },
      "item_id": { "type": "string" },
      "qty": { "type": "number", "minimum": 1 }
    },
    "required": ["tab_id", "item_id", "qty"]
  },
  "handlerPath": "./handler.ts"
}


