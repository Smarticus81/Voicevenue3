{
  "name": "inventory.get_level",
  "description": "Get on-hand stock and par for an ingredient at a venue.",
  "parameters": {
    "type": "object",
    "properties": {
      "ingredient_id": { "type": "string" }
    },
    "required": ["ingredient_id"]
  },
  "handlerPath": "./handler.ts"
}


